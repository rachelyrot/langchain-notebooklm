# Copy to .env and fill in. Loaded automatically via python-dotenv.

# --- Chat model (default provider: Anthropic) ---
ANTHROPIC_API_KEY=sk-ant-...
# Override the default chat model if you like:
# NOTEBOOKLM_CHAT_MODEL=anthropic:claude-sonnet-4-6

# --- Embeddings (default provider: Cohere) ---
COHERE_API_KEY=...

# --- Web research (Firecrawl: search + scrape + crawl) ---
# Get a key at https://firecrawl.dev. Without it the app still runs; only the
# "Research the web" button returns 503.
FIRECRAWL_API_KEY=fc-...
# NOTEBOOKLM_EMBEDDING_PROVIDER=cohere
# NOTEBOOKLM_EMBEDDING_MODEL=embed-multilingual-v3.0
