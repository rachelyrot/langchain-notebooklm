# Screenshots

`screenshot.png` is the image the project README leads with: the three-panel workspace
with a few sources loaded and an answer showing its citations.

To retake it: run the server, load a handful of sources, ask a question that cites two or
three of them, and capture the whole window.
